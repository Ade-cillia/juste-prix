<!DOCTYPE html>

<html>
<head>
    <title>Jeu</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>

<div class="container text-center">

    <h1>LE JEU DU JUSTE PRIX</h1>

    <div class="row col-8 mt-4 mx-auto">

        <div class="card shadow">

            <form action="/game/check/" method="post" class="card-body" id="formPlayGame">

                <div class="mb-4" id="info">
                    Bonjour <%= playerTwo %> , vous devez trouver la valeur de l'objet suivant ("<%= objectName %>") en un minimum de tentatives:
                </div>

                <div class="input-group mb-3">
                    <input type="number" name="newTry" class="form-control" placeholder="Quel prix vous semble être juste ?" id="playerTwoTry" <% if(isWin) { %> disabled <% } %>>
                    <button type="submit" id="submitFormPlayGame" class="btn btn-outline-secondary">Faire une tentative</button>
                </div>

                <div id="tries">
                    <p>Nombre d'essaie : <%= numberTry%></p>
                    
                    <% if(tryResponse === "null") { %>
                        <p class="alert-warning">La valeur de l'objet n'est pas null</p>
                    <% } else if(tryResponse === "up") { %>
                        <p class="alert-warning">La valeur de l'objet est plus haute</p>
                    <% } else if(tryResponse === "down") { %>
                        <p class="alert-warning">La valeur de l'objet est plus basse</p>
                    <% } else if(tryResponse === "win" && isWin) { %>
                        <p class="alert-success">Félicitations ! Tu as trouvé la bonne valeur en seulement <%= numberTry %> tentative(s)</p>
                    <% } %>
                    
                    
                    
                </div>
                <a href="/" type="button" id="replayGame" class="btn btn-dark" <% if(isWin === false) { %> hidden <% } %>>Rejouer</a>

            </form>

        </div>

    </div>

</div>

</body>
</html>
